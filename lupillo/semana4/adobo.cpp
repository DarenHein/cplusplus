#include <iostream>
using namespace std; 

int main(){

    // matrices 
    int filas = 2; 
    int columnas = 2 ;
    int matriz[filas][columnas]; 

    for (int i = 0 ; i < filas ; i ++){
        for (int j = 0 ; j < columnas ; j ++ ){
            cout << "Ingresa los elementos de la matriz ["<<i<<"] ["<< j << "] -> "; 
            cin >> matriz [i][j]; 
        }
    }
    // es mostrarme la matriz ingresada 
     for (int i = 0 ; i < filas ; i ++){
        for (int j = 0 ; j < columnas ; j ++ ){
            cout << "Ingresa los elementos de la matriz ["<<i<<"] ["<< j << "] -> "; 
            cin >> matriz [i][j]; 
        }
    }
    for (int i = 0 ; i < filas ; i ++){
        for (int j = 0 ; j < columnas ; j ++ ){
            cout << 
        }
    }

    return 0; 
}